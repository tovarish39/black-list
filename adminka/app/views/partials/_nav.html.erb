<nav class='nav'>
    <ul>
        <% for link in @links  %>
            <li>
               <%= link_to link[:label], link[:path],class:"nav__link nav__link#{active?(link[:path])}" %> 
            </li>  
        <% end%>
    </ul>
</nav>