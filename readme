.env
adminka/.env
in adminka -> bundle exec whenever --update-crontab

database.yml

in telegram-bot-ruby 
module Telegram
  module Bot
    module Types
      class ChatMember < Base
        attribute :status, Types::String
      end
    end
  end
end

rails c
Counter.create

command to main bot for change video in private channel for scammers
=>  /config channel-video